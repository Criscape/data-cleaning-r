# Tipos de datos en R
# Enteros
a <- 5
b <- 1:10
# Numéricos
c <- 3.14
d <- c(1.5, 2.5, 3.34, 10.6576)
# Cadenas
string <- "my text"
vec_string <- c("hello", "world", "of", "R")
# Lógicos o booleanos
flag <- TRUE

# Estructuras de datos
# Vectores
vec <- c(1, 2, 3, 4, 5)
# Matrices
mat <- matrix(1:9, nrow=3, ncol=3)
# Data Frames
df <- data.frame(id=1:3, name=c("Alice", "Bob", "Charlie"), score=c(85.5, 90.0, 78.5))
# Listas
my_list <- list(name="John", age=30, scores=c(90, 85, 88))
my_list2 <- list(1, "text", TRUE, c(1,2,3), matrix(1:4, nrow=2))
